<!-- -->



<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Super App Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">Super App</a>
      <div>
        <a class="nav-link d-inline text-white" href="/">Home</a>
        <a class="nav-link d-inline text-white" href="/blog">Blog</a>
        <a class="nav-link d-inline text-white" href="/blog/new">New blog</a>
        <a class="nav-link d-inline text-white" href="/about">About</a>
      </div>
    </div>
  </nav>

  <div class="container mt-5">
    <div class="text-center mb-4">
      <h1 class="fw-bold">📝 This is Super App Blog</h1>
      <p class="text-muted">Read, edit or delete your posts easily.</p>
    </div>

    <% if (blogs.length === 0) { %>
      <div class="alert alert-info text-center">No blogs yet. <a href="/blog/new" class="alert-link">Add one</a>.</div>
    <% } %>

    <div class="row justify-content-center">
      <% blogs.forEach(blog => { %>
        <div class="col-md-8">
          <div class="card shadow-sm mb-4 border-0">
            <div class="card-body">
              <h4>
                <a href="/blog/<%= blog._id %>" class="text-decoration-none text-primary fw-semibold">
                  <%= blog.title %>
                </a>
              </h4>
              <p class="text-muted mb-3">
                <%= blog.createdAt.toDateString() %>
              </p>

              <!-- <a href="/blog/edit/<%= blog._id %>" class="btn btn-warning btn-sm me-2"> Edit</a> -->
              <form action="/blog/<%= blog._id %>?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger btn-sm"> Delete</button>
              </form>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <footer class="text-center py-4 mt-5 bg-dark text-white">
    <p class="mb-0">&copy; <%= new Date().getFullYear() %> Super App Blog | Created with ❤️ by Olimjon</p>
  </footer>

</body>
</html>
